diff --git a/enn_torch_dev/executor/state.py b/enn_torch_dev/executor/state.py
index dfd6a95..9ff3d33 100644
--- a/enn_torch_dev/executor/state.py
+++ b/enn_torch_dev/executor/state.py
@@ -137,6 +137,16 @@ class StateRoute:
         store.set(self.return_state_key, True)
         return store

+    def reset(self, store: KVStore, *, missing_ok: bool = True) -> KVStore:
+        """Delete the routed input state slot from ``store``."""
+        if not isinstance(store, KVStore):
+            raise TypeError(f"StateRoute.reset expects KVStore, got {type(store)!r}")
+        if not isinstance(missing_ok, bool):
+            raise TypeError("StateRoute.reset missing_ok must be a bool.")
+
+        store.delete(self.state_input_key, missing_ok=missing_ok)
+        return store
+
     @staticmethod
     def _carried_payload(
         payload: Any,
diff --git a/enn_torch_dev/executor/stream.py b/enn_torch_dev/executor/stream.py
index c1dc399..78b8c49 100644
--- a/enn_torch_dev/executor/stream.py
+++ b/enn_torch_dev/executor/stream.py
@@ -67,6 +67,7 @@ class StreamPipelineSpec:
     outputs_key: str | None = None
     state_detach: bool = False
     state_clone: bool = False
+    reset_state: bool = False

     def __post_init__(self) -> None:
         self.chunk_input_key = _validate_stream_key(
@@ -95,6 +96,7 @@ class StreamPipelineSpec:
             )
         self.state_detach = _validate_stream_bool(self.state_detach, "state_detach")
         self.state_clone = _validate_stream_bool(self.state_clone, "state_clone")
+        self.reset_state = _validate_stream_bool(self.reset_state, "reset_state")


 class StreamPipeline:
@@ -142,6 +144,10 @@ class StreamPipeline:
         result_key = self._result_key()
         outputs: list[Any] = []

+        if self.spec.reset_state:
+            for route in self.state_routes:
+                route.reset(store, missing_ok=True)
+
         for index, chunk in enumerate(normalized_chunks):
             chunk_store = store.fork()
             chunk_store.set(self.spec.chunk_input_key, chunk, origin="StreamPipeline")
diff --git a/enn_torch_dev/debug/executor/test_state_routing.py b/enn_torch_dev/debug/executor/test_state_routing.py
index 83db602..5c63733 100644
--- a/enn_torch_dev/debug/executor/test_state_routing.py
+++ b/enn_torch_dev/debug/executor/test_state_routing.py
@@ -68,6 +68,39 @@ def test_state_route_enable_return_state_writes_flag() -> None:
     assert store.get("flag") is True


+def test_state_route_reset_deletes_input_state() -> None:
+    route = StateRoute("ctx.state.in", "ctx.state.out")
+    store = KVStore({"ctx.state.in": torch.zeros(1, 2, 3)})
+
+    returned = route.reset(store)
+
+    assert returned is store
+    assert not store.has("ctx.state.in")
+
+
+def test_state_route_reset_missing_is_noop_by_default() -> None:
+    route = StateRoute("ctx.state.in", "ctx.state.out")
+    store = KVStore()
+
+    returned = route.reset(store)
+
+    assert returned is store
+    assert not store.has("ctx.state.in")
+
+
+def test_state_route_reset_missing_can_raise() -> None:
+    route = StateRoute("ctx.state.in", "ctx.state.out")
+
+    with pytest.raises(KeyError, match="ctx.state.in"):
+        route.reset(KVStore(), missing_ok=False)
+
+
+def test_state_route_reset_validates_inputs() -> None:
+    route = StateRoute("ctx.state.in", "ctx.state.out")
+
+    with pytest.raises(TypeError, match="KVStore"):
+        route.reset(object())
+
+    with pytest.raises(TypeError, match="missing_ok"):
+        route.reset(KVStore(), missing_ok=1)
+
+


 def test_state_route_can_make_state_input_required() -> None:
diff --git a/enn_torch_dev/debug/executor/test_stream.py b/enn_torch_dev/debug/executor/test_stream.py
index 80ab0f6..f34823a 100644
--- a/enn_torch_dev/debug/executor/test_stream.py
+++ b/enn_torch_dev/debug/executor/test_stream.py
@@ -294,6 +294,55 @@ def test_stream_pipeline_can_clone_carried_state() -> None:
     assert store.get("sum.state.in").data_ptr() != outputs[0].data_ptr()


+def test_stream_pipeline_reset_state_clears_previous_stream_state() -> None:
+    route = StateRoute("sum.state.in", "sum.state.out")
+    graph = GraphExecutor()
+    graph.add_node(
+        NodeSpec(
+            name="sum",
+            input_args=[KeyRef("chunk.x")],
+            input_kwargs=route.input_kwargs(),
+            output_key="sum.out",
+            output_keys=route.output_keys("sum.out"),
+        ),
+        _RunningSum(),
+    )
+    pipeline = StreamPipeline(
+        graph,
+        StreamPipelineSpec(
+            chunk_input_key="chunk.x",
+            output_name="sum",
+            reset_state=True,
+        ),
+        state_routes=[route],
+    )
+    store = KVStore({"sum.state.in": torch.tensor([100.0])})
+
+    outputs = pipeline.run(store, [torch.tensor([1.0]), torch.tensor([2.0])])
+
+    assert [out.item() for out in outputs] == [1.0, 3.0]
+    assert torch.equal(store.get("sum.state.in"), torch.tensor([3.0]))
+
+
+def test_stream_pipeline_keeps_previous_state_without_reset_state() -> None:
+    route = StateRoute("sum.state.in", "sum.state.out")
+    graph = GraphExecutor()
+    graph.add_node(
+        NodeSpec(
+            name="sum",
+            input_args=[KeyRef("chunk.x")],
+            input_kwargs=route.input_kwargs(),
+            output_key="sum.out",
+            output_keys=route.output_keys("sum.out"),
+        ),
+        _RunningSum(),
+    )
+    pipeline = StreamPipeline(
+        graph,
+        StreamPipelineSpec(chunk_input_key="chunk.x", output_name="sum"),
+        state_routes=[route],
+    )
+    store = KVStore({"sum.state.in": torch.tensor([100.0])})
+
+    outputs = pipeline.run(store, [torch.tensor([1.0]), torch.tensor([2.0])])
+
+    assert [out.item() for out in outputs] == [101.0, 103.0]
+    assert torch.equal(store.get("sum.state.in"), torch.tensor([103.0]))
+
+
 def test_stream_pipeline_allows_empty_chunks() -> None:
     pipeline = StreamPipeline(
         _make_double_graph(),
@@ -340,6 +389,9 @@ def test_stream_pipeline_validates_inputs() -> None:
     with pytest.raises(TypeError, match="state_clone"):
         StreamPipelineSpec(chunk_input_key="chunk.x", output_name="double_node", state_clone=1)

+    with pytest.raises(TypeError, match="reset_state"):
+        StreamPipelineSpec(chunk_input_key="chunk.x", output_name="double_node", reset_state=1)
+
     with pytest.raises(TypeError, match="GraphExecutor"):
         StreamPipeline(
             object(),
