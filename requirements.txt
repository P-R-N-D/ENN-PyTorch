# Notes
# - Install the correct PyTorch build for your accelerator (CUDA/ROCm/XPU/CPU)
#   **before** installing this project.
# - Python >= 3.10 is required; Python 3.14t (free-threading) is recommended for
#   higher data pipeline throughput.
#
# This file is kept as a convenience for environments that still prefer `pip install -r requirements.txt`.
# The canonical dependency set is in `pyproject.toml`.

# Base runtime (mirrors pyproject.toml -> [project].dependencies)
#
# If error happens when building wheel of openzl, install this using the official GitHub repository by Meta Platforms, Inc.
# `pip install "openzl @ git+https://github.com/facebook/openzl.git#subdirectory=py"`
numpy>=2.4.1
psutil>=7.2.1
py-cpuinfo>=9.0.0
torch>=2.9.1
torchvision>=0.24.1
torchaudio>=2.9.1
torchao>=0.15.0
torchdata>=0.11.0
tensordict>=0.10.0
tqdm>=4.67.1
h5py>=3.15.1
triton>=3.5.1
onnx>=1.20.1
onnxruntime>=1.23.2
onnxscript>=0.5.7
onnxoptimizer>=0.4.2
onnx_ir>=0.1.14
openzl>=0.1.0
ml_dtypes>=0.5.4

# Checkpoint / IO formats (optional)
# - safetensors: enables .safetensors checkpoints
# safetensors>=0.4.0

# Optional extras (install via `pip install -e .[extra]`)
#
# DataFrame integrations
# - pandas: pandas>=2.2.0, openpyxl>=3.1.5, fastexcel>=0.14.0,
#   xlsxwriter>=3.2.0
# - polars: polars>=1.30.0, openpyxl>=3.1.5, fastexcel>=0.14.0,
#   xlsxwriter>=3.2.0
# - pandas_on_spark: pyspark[pandas_on_spark]>=4.0.0, openpyxl>=3.1.5,
#   fastexcel>=0.14.0, xlsxwriter>=3.2.0
#
# Hardware / optimizer backends
# - torchao: torchao>=0.15.0
# - nvidia_te_cu12: transformer-engine-cu12>=2.2.0,
#   transformer-engine-torch>=2.2.0
# - nvidia_te_cu13: transformer-engine-cu13>=2.2.0,
#   transformer-engine-torch>=2.2.0
# - intel_ai: intel-extension-for-pytorch>=2.7.0
#
# Export / deployment (optional)
# - deployment_full: onnx2tf>=1.27.2 (py<3.13), tf-keras>=2.20.1,
#   tensorflow>=2.20.0, coremltools>=8.3.0 (macOS),
#   tensorrt>=10.10.0.31 (Linux/CUDA), executorch>=0.6.0 (Linux, py<3.12;
#   wheel availability varies)
#
# Telemetry
# - telemetry: nvidia-ml-py>=11.515.48
#
# NUFFT losses (CUDA)
# - nufft: cufinufft
