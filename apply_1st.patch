diff --git a/enn_torch/runtime/distributed.py b/enn_torch/runtime/distributed.py
--- a/enn_torch/runtime/distributed.py
+++ b/enn_torch/runtime/distributed.py
@@
 def get_control_process_group(pg: ProcessGroup | None = None) -> ProcessGroup | None:
     if not is_distributed():
         return None
     cpg = get_cpu_group()
     if cpg is not None:
         return cpg
-    if pg is not None:
-        try:
-            p_be = str(dist.get_backend(pg)).lower()
-            if (p_be == "gloo") or ("cpu:gloo" in p_be):
-                return pg
-        except Exception:
-            pass
-        return pg
-    try:
-        w_be = str(dist.get_backend(dist.group.WORLD)).lower()
-        if (w_be == "gloo") or ("cpu:gloo" in w_be):
-            return dist.group.WORLD
-    except Exception:
-        pass
-    return dist.group.WORLD
+    # If caller provided a CPU-capable subgroup, honor it. Do NOT return a non-CPU
+    # subgroup here: call sites that use control collectives will typically use CPU
+    # tensors, which will error on NCCL/XCCL/HCCL groups and can deadlock if only a
+    # subset participates.
+    if pg is not None:
+        try:
+            p_be = str(dist.get_backend(pg)).lower()
+            if (p_be == "gloo") or ("cpu:gloo" in p_be):
+                return pg
+        except Exception:
+            # If we cannot identify the backend, avoid assuming CPU-capable.
+            pass
+        # fall through: try WORLD if it is CPU-capable, else return None
+    try:
+        w_be = str(dist.get_backend(dist.group.WORLD)).lower()
+        if (w_be == "gloo") or ("cpu:gloo" in w_be):
+            return dist.group.WORLD
+    except Exception:
+        pass
+    return None
